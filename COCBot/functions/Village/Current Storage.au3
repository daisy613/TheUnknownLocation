;this in not complete as im nmot good enough, lol

;i need to get the current gold and elixir in the village, this will then be compared to the
;minimum that the user specified to start building walls at.
;$VillageGold = current gold
;$VillageElixir = current elixir
;$txtWallMinGold = the stored number specified as per config
;$txtWallMinElixir = he stored number specified as per config



;Local $VillageGold = getOther(700, 24, "Current Gold")
	  ;While Number($VillageGold) > Number($txtWallMinGold)
		;$VillageGold = getOther(700, 24, "Current Gold")
		;SetLog("Current Gold : " & $VillageGold, $COLOR_GREEN)
		;If Number($VillageGold) > Number($txtWallMinGold) Then
			;SetLog("Upgrading Walls Using Gold", $COLOR_BLUE)


;Local $VillageElixir($VillageElixir) > Number($txtWallMinElixir)
		;$VillageElixir = getOther(700, 74, "Current Elixir")
		;SetLog("Current Elixir : " & $VillageElixir, $COLOR_GREEN)
		;If Number($VillageElixir) > Number($txtWallMinElixir) Then
			;SetLog("Upgrading Walls Using Elixir", $COLOR_BLUE)


